@page
@model DAWPI.Pages.Administrador.BorrarModel
@{
    ViewData["Title"] = "Borrar";
}

@if (Model.usuarioDTO != null)

{
    <form method="post" style="background-color: rgba(245, 245, 245, 0.9); padding: 10px; border-radius: 10px; box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);">
        <div class="container">
            <h2 style="font-size: 24px; margin-bottom: 10px;">Confirmar borrado</h2>
            <p>Por favor, confirma que deseas borrar el siguiente usuario:</p>
            <p><strong>Nombre Completo:</strong> @Model.usuarioDTO.NombreCompleto</p>
            <p>Introduce el nombre completo del usuario para confirmar el borrado:</p>
            <div class="form-group">
                <input type="text" name="confirmacion" id="confirmacion" class="form-control" />
            </div>
            <br />
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger">
                    <ul>
                        @foreach (var modelState in ViewData.ModelState.Values)
                        {
                            foreach (var error in modelState.Errors)
                            {
                                <li>@error.ErrorMessage</li>
                            }
                        }
                    </ul>
                </div>
            }
            <br />
            <button type="submit" class="btn btn-danger">Borrar usuario</button>
        </div>
    </form>
}
else
{
    <div class="alert alert-danger">
        <h4>Aún no ha seleccionado ningun paciente o médico, o este no existe.</h4>
        <p>
            Por favor, asegurese de seleccionar un paciente o médico.
        </p>
    </div>
}

